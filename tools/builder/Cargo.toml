[package]
name = "builder"
description = "An internal build tool that extracts game metadata from example packages and generates a JSON manifest for the Redixel Web frontend."
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
publish = false

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
toml = "1.1.2"